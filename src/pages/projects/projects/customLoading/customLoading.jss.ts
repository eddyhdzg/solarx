import { makeStyles } from "@material-ui/core/styles";

const useStyles = makeStyles(() => ({
  customLoading_root: {
    position: "absolute",
    top: 0,
    width: "100%",
  },
}));

export default useStyles;
