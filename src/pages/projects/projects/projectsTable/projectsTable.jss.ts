import { makeStyles } from "@material-ui/core/styles";

const useStyles = makeStyles(() => ({
  projectsTable_paper: {
    display: "flex",
    whiteSpace: "nowrap",
    overflow: "scroll",
  },
}));

export default useStyles;
