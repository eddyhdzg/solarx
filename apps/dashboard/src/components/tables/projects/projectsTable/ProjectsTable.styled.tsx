import { styled, Paper, TableHead } from "@mui/material";

export const StyledPaper = styled(Paper)({
  display: "flex",
  whiteSpace: "nowrap",
  overflow: "auto",
});

export const StyledTableHead = styled(TableHead)({
  textTransform: "capitalize",
});
